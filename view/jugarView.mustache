{{>header}}

<link rel="stylesheet" href="/PW2-TP2/public/styles/jugar.css">

<div class="container">
    <div class="question-card">
        <div class="category-bar" style="background: {{color}}">
            {{categoria}}
        </div>

        <div class="question-header">
            <span>Puntaje: {{puntaje}}</span>
        </div>

        {{#mensaje}}
            <div class="message">
                {{mensaje}}
            </div>
        {{/mensaje}}

        <h2 class="question-text">
            {{pregunta}}
        </h2>

        <form method="POST" action="/PW2-TP2/partida/responder">
            {{#opciones}}
            <button type="submit" name="respuesta" value="{{valor}}" class="option-btn">
                <span class="option-letter">{{letra}}</span>
                <span class="option-text">{{texto}}</span>
            </button>
            {{/opciones}}
        </form>
    </div>
</div>

{{>footer}}